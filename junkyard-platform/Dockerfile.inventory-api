FROM python:3.13-slim
WORKDIR /app
COPY inventory_api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY inventory_api/ ./inventory_api/
EXPOSE 8100
CMD ["uvicorn", "inventory_api.main:app", "--host", "0.0.0.0", "--port", "8100"]

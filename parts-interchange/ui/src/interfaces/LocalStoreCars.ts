export interface LocalStoreCarList {
    cars: number[];
}
export interface LocalStoreCar {
    year: string;
    make: string;
    model: string;
    trim: string;
    engine: string;
    id: number;
}
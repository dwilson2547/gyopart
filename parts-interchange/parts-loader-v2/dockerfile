FROM python:3.11-slim

WORKDIR /app
COPY src/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY src/ .

# Default entrypoint — override CMD to run load_csvs.py instead
CMD ["python", "generate_csvs.py"]

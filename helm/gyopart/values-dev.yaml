namespace:
  create: true
  name: gyopart-dev

domain: gyopart-dev.local

imagePullPolicy: Always

postgres:
  host: postgres-dev.postgres.svc.cluster.local
  junkyardSecret: junkyard-credentials
  partsSecret: parts-interchange-credentials
